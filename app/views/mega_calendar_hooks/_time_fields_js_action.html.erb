
<% if @issue.safe_attribute?('start_date') || @issue.safe_attribute?('due_date') %>
  <script type="text/javascript">
    $('.clockpicker').clockpicker({
      placement: 'top',
      align: 'left',
      donetext: '<%= t(:clockpicker_done_text) %>'
    });
  </script>
<% end %>